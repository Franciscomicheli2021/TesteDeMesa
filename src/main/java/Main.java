public class Main {
  boolean opcao = false;
  int numero = 1;
  String texto = "1";

  boolean resultado = (opcao && !opcao);

  numero = (numero + 2);

  resultado = (numero >= 2) || resultado;

  numero = 1;

  reultado = (numero == texto) && resultado;
  System.out.println(resultado);

  
}